# Examples

This directory is the fastest way to understand the product without running it end to end.

- `benchmark_inputs/`: upload-ready benchmark JSON files
- `analysis_outputs/`: representative persisted output shapes
- `dashboard_views/`: visual artifacts and screenshot placeholders

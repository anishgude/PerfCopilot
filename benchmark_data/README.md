# Benchmark Data

This directory keeps benchmark inputs visible and easy to inspect.

- `samples/`: hand-curated benchmark payloads for local testing and demos
- `historical/`: benchmark artifacts used by automation flows such as the PR bot

Each sample follows the same JSON schema consumed by the backend API.
